/**
 * @date 2024/4/13
 */
import { ui } from "@ui/layaMaxUI";

export class LoginMdr extends ui.login.LoginUI {
  onEnable() {
    super.onEnable();
  }

  onOpened(param: any) {
    super.onOpened(param);
  }

  close(type?: string) {
    super.close(type);
  }
}
